# Getting Started

Status: Draft

This page will guide a new user through code access, MATLAB path setup, the
first `Dataset("Iris")` construction, and the smoke workflow.

Planned coverage:

- Requesting access to the private code repository.
- Adding the repository root to MATLAB path.
- Loading a small public dataset.
- Running a minimal benchmark.
- Running `examples/smokeExpManner.m`.
