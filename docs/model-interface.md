# Model Interface

Status: Draft

This page will explain how project-side clustering models connect to ExpManner.

Planned coverage:

- Duck-typed model interface.
- `requiredInitFields()`.
- `train(ds, initState)`.
- Optional `ModelBase`.
- Prediction outputs: labels, membership, embedding, and affinity.
