# FAQ

Status: Draft

This page will collect common questions about ExpManner.

Planned coverage:

- Why there is no `+ExpManner` package namespace.
- Why project-side models stay outside the framework core.
- Why `Metricer` and `utils` use class folders.
- Why generated results are not committed.
- Why code is private while docs are public.
