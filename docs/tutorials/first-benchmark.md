# First Benchmark

Status: Draft

This tutorial will run the first public-safe benchmark with a small dataset and
demo model.

Planned coverage:

- `Dataset("Iris", Normalize="range")`.
- `DemoModels.KMeans()`.
- `TaskManner.train`.
- Summary table inspection.
- Generated results.
