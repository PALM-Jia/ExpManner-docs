# Dataset Guide

Status: Draft

This page will explain feature datasets, ensemble datasets, dataset name hints,
and safe public documentation boundaries for private datasets.

Planned coverage:

- `Dataset.registry()`.
- `Dataset.ensembleRegistry()`.
- `Dataset.names()` and `Dataset.suggestName()`.
- `Kind="feature"` and `Kind="ensemble"`.
- Dataset containers.
